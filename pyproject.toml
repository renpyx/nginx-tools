[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "nginx-tool"
version = "0.0.1-Alpha"
dependencies = [
    "argcomplete",
]

[project.scripts]
nxtool = "app:main"